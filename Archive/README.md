# Archive

Move-only graveyard. Nothing here is deleted — restore by moving back.

## brainstorming/ (added 2026-07-03)

Three Superpowers brainstorm sandboxes recovered from `Projects\.superpowers\brainstorm\`
(created 2026-04-25, when the brainstorm ran from the Projects root instead of this repo).
All are cortex origin-design artifacts:

| Sandbox | Contents |
|---|---|
| `43834-1777059708/` | Origin brainstorm — "link brain" concept: platform choice (extension vs local app vs …), Kanban vs inbox approaches, card designs, layout mockups v1–v3 |
| `45779-1777064636/` | design-v3 iteration |
| `47115-1777107083/` | Hover-effect exploration + `hover-final.html` ("Cortex — Final Hover Effect") |

`state/server.pid` files are dead — sandboxes' preview servers long stopped.
Live source of truth: cortex app code in this repo.
